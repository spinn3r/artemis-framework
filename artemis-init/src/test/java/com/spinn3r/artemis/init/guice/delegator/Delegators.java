package com.spinn3r.artemis.init.guice.delegator;

import com.google.common.collect.Lists;
import com.spinn3r.artemis.init.delegate.Delegatable;

import java.net.ConnectException;
import java.util.List;

/**
 *
 */
public class Delegators {

//    public static class Connector<T> {
//
//        //private <Delegatable> List<D>
//
//        public <R extends Delegatable<T> & T> Connector<T> and( Delegatable<T> delegatable ) {
//
//
//        }
//
//    }

//    public static <T extends Delegatable<T>> Connector<T> connect( T first ) {
//        return new Connector<>( first );
//    }

//    public static class Connector<T, D extends T , E extends Delegatable<T>> {
//
//        private T type;
//
////        private <D extends Delegatable<T>> List<D>
//
//        public Connector(T type) {
//            this.type = type;
//        }
//
//        public <C extends D & E> Connector<T> and( Delegatable<T> next ) {
////            delegatables.add( next );
//
//            return this;
//        }
////
////        public T to( T target ) {
////
////            for (int i = 0; i < delegatables.size() - 1; i++) {
////
////                Delegatable<T> current = delegatables.get( i );
////                Delegatable<T> next = delegatables.get( i + 1 );
////
////                current.setDelegate( next );
////
////            }
////
////        }
//
//    }

}
